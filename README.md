# ⏰ Local Time Display App

A simple application that fetches the current time from the user’s local machine and displays it on the screen in real time.

---

## 🚀 Features

- Displays current local time
- Automatically updates every second
- Uses the system’s local time
- Lightweight and beginner-friendly

---

## 🛠️ Technologies Used

- HTML
- CSS
- JavaScript

---

## ▶️ How It Works

- The app uses JavaScript’s `Date` object to get the current local time.
- The time is updated every second using `setInterval`.
- The updated time is displayed dynamically on the screen.

---

